extends layout

block content

    ul
        each brand in brand_list
            li
                a(href=brand.url) #{brand.logo}
                |   (#{brand.name})
        else
            li There are no brands